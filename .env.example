# ============================================
# REQUIRED: Supabase Configuration
# ============================================
# Get these from your Supabase project settings:
# https://app.supabase.com/project/_/settings/api

# Public URL - exposed to client (PUBLIC_ prefix makes it available in browser)
PUBLIC_SUPABASE_URL=your_supabase_url

# Anonymous key - exposed to client (safe for public use with RLS)
PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Service role key - NEVER expose to client (server-only, bypasses RLS)
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key


# ============================================
# OPTIONAL: Redis Configuration (Rate Limiting)
# ============================================
# Required in production for rate limiting
# Get these from Upstash: https://upstash.com/
# Optional in development (rate limiting will be disabled)

UPSTASH_REDIS_URL=your_redis_url
UPSTASH_REDIS_TOKEN=your_redis_token


# ============================================
# OPTIONAL: Sentry Configuration (Error Tracking)
# ============================================
# Recommended for production error monitoring
# Get this from Sentry: https://sentry.io/
# Optional in development (errors will log to console)

SENTRY_DSN=your_sentry_dsn


# ============================================
# OPTIONAL: OAuth Configuration
# ============================================
# Required only if you want to enable OAuth login
# Configure in Supabase Auth settings first

# Google OAuth
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret

# GitHub OAuth
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret


# ============================================
# Development Notes
# ============================================
# 1. Copy this file to .env and fill in your values
# 2. Never commit .env to version control
# 3. PUBLIC_ prefixed variables are exposed to the browser
# 4. Non-prefixed variables are server-only
# 5. In production, set these in your Vercel project settings
